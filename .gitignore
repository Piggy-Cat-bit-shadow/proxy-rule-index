# 项目文件
__pycache__/
*.pyc
.venv/
venv/
.pytest_cache/

# 运行时缓存
cache/
tmp/
*.tmp

# 本地报告 (构建时生成, 无需入库)
reports/

# deploy-site/ 是开发预览目录, 部署时由 dist/ 承担
deploy-site/

# .snapshot/ 是工作副本, 可从 generated/ shards 恢复, 不入库
.snapshot/

# generated/ 和 dist/ 需要入库: CI 增量复用 + Pages 部署都依赖它们
